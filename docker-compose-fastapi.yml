version: '3'

services:
  fastapi:
    build:
      context: .
      dockerfile: dockers/fastapi/Dockerfile
    volumes:
      - ./model:/app/model
    ports:
      - 8100:8100
    command: uvicorn api_main:app --reload --host 0.0.0.0

